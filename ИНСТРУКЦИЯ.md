# 📋 Пошаговая инструкция по развертыванию системы аудита логов

## 🎯 Обзор системы

Система состоит из трех компонентов:
- **Сервер** - центральная машина для приема и хранения логов с веб-дашбордом (1 машина)
- **Агенты** - собирают логи на удаленных машинах (от 1 до N машин)
- **Клиент-аудитор** - веб-дашборд (доступен через браузер) или десктопный GUI (1 машина, может быть та же, что и сервер)

**Минимальная конфигурация:** 1 машина (сервер + веб-дашборд на одной машине)
**Рекомендуемая конфигурация:** 2+ машины (1 сервер + N агентов)

**Веб-дашборд** доступен через браузер по адресу `http://<IP-сервера>:8080` и включает:
- Карточки ключевых метрик
- Интерактивные графики и диаграммы
- Таблицу событий с фильтрацией
- Автоматическое обновление данных

---

## 📦 Шаг 1: Подготовка файлов для переноса

### 1.1. Определите, какие файлы нужны для каждой машины

**Для СЕРВЕРА нужны файлы:**
```
main.py
requirements-server.txt
server/
  ├── analyzer.py
  ├── fetcher.py
  ├── parser.py
  └── storage.py
web/
  ├── index.html
  └── static/
      ├── style.css
      └── dashboard.js
```

**Для АГЕНТА нужны файлы:**
```
main.py
requirements-client.txt
client/
  ├── agent.py
  ├── connection.py
  └── gui.py (не используется агентом, но нужен для совместимости)
```

**Для КЛИЕНТА-АУДИТОРА нужны файлы:**
```
main.py
requirements-client.txt
client/
  ├── connection.py
  └── gui.py
```

### 1.2. Способы переноса файлов

**Вариант A: Копирование через USB/сеть**
- Скопируйте нужные папки и файлы на целевую машину
- Сохраните структуру папок (server/, client/)

**Вариант B: Через Git (если есть репозиторий)**
```bash
git clone <URL-репозитория>
cd ПО
```

**Вариант C: Через сетевую папку**
- Откройте общую папку на целевой машине
- Скопируйте файлы

---

## 🖥️ Шаг 2: Настройка СЕРВЕРА (1 машина)

### 2.1. Выберите машину для сервера
- Рекомендуется: Linux-машина
- Альтернатива: Windows/macOS (тоже работает)
- Убедитесь, что машина доступна по сети для агентов

### 2.2. Перенесите файлы на сервер
Скопируйте на сервер:
- `main.py`
- `requirements-server.txt`
- Папку `server/` (со всеми файлами внутри)
- Папку `web/` (со всеми файлами внутри) - для веб-дашборда

**Структура на сервере должна быть:**
```
/путь/к/проекту/
  ├── main.py
  ├── requirements-server.txt
  ├── server/
  │   ├── analyzer.py
  │   ├── fetcher.py
  │   ├── parser.py
  │   └── storage.py
  └── web/
      ├── index.html
      └── static/
          ├── style.css
          └── dashboard.js
```

### 2.3. Проверьте Python на сервере
```bash
# На Linux/macOS:
python3 --version

# На Windows:
python --version
```
**Требуется:** Python 3.10 или выше

Если Python не установлен:
- **Windows:** Скачайте с [python.org](https://www.python.org/downloads/)
- **Linux:** `sudo apt install python3 python3-pip` (Ubuntu/Debian) или `sudo yum install python3 python3-pip` (CentOS/RHEL)

**⚠️ ВАЖНО для Ubuntu 22.04:**
В Ubuntu 22.04 pip **не установлен по умолчанию**. Если вы получили ошибку `No module named pip`, выполните:
```bash
# Обновите список пакетов
sudo apt update

# Установите Python и pip
sudo apt install python3 python3-pip -y

# Проверьте установку
python3 --version
python3 -m pip --version
```
После этого можно переходить к шагу 2.4.

### 2.4. Установите зависимости на сервере
```bash
# На Linux/macOS:
cd /путь/к/проекту
python3 -m pip install -U pip
python3 -m pip install -U -r requirements-server.txt

# На Windows:
cd C:\путь\к\проекту
python -m pip install -U pip
python -m pip install -U -r requirements-server.txt
```

### 2.5. Запустите сервер
```bash
# На Linux/macOS:
python3 main.py server --host 0.0.0.0 --port 8080

# На Windows:
python main.py server --host 0.0.0.0 --port 8080
```

**Параметры:**
- `--host 0.0.0.0` - сервер доступен со всех сетевых интерфейсов (для доступа агентов)
- `--port 8080` - порт сервера (можно изменить)
- `--db logs.db` - путь к базе данных (по умолчанию `logs.db`)

**Ожидаемый вывод:**
```
INFO:     Started server process [12345]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8080 (Press CTRL+C to quit)
```

### 2.6. Запишите IP-адрес сервера
Узнайте IP-адрес сервера:
```bash
# На Linux:
hostname -I
# или
ip addr show

# На Windows:
ipconfig
```

**Пример:** `192.168.1.100` - этот адрес понадобится для агентов и клиента.

### 2.7. Проверьте доступность сервера
Откройте в браузере или выполните:
```bash
curl http://<IP-сервера>:8080/health
```

Должен вернуться ответ: `{"status":"ok"}`

### 2.8. Доступ к веб-дашборду
После запуска сервера веб-дашборд доступен по адресу:
- **Локально:** `http://localhost:8080`
- **Удаленно:** `http://<IP-сервера>:8080`

**Веб-дашборд включает:**
- 📊 **Карточки ключевых метрик:**
  - Процент критических событий
  - Количество критических событий
  - Прогресс анализа
  - Прогресс обработки критических событий

- 📈 **Интерактивные графики:**
  - Распределение событий по уровням важности (круговая диаграмма)
  - Тепловая карта событий (severity × likelihood)
  - Распределение по статусам (donut chart)
  - Топ-5 хостов с критическими событиями
  - Топ-5 уровней важности

- 📋 **Таблица событий:**
  - Фильтрация по уровню важности
  - Поиск по содержимому
  - Автоматическое обновление каждые 30 секунд

**Преимущества веб-дашборда:**
- ✅ Доступен из любого браузера (не требует установки GUI)
- ✅ Работает на любой платформе (Windows, Linux, macOS)
- ✅ Современный и интуитивный интерфейс
- ✅ Адаптивный дизайн (работает на планшетах и телефонах)
- ✅ Автоматическое обновление данных

---

## 🤖 Шаг 3: Настройка АГЕНТОВ (1 или более машин)

### 3.1. Определите количество агентов
- Агент устанавливается на каждую машину, с которой нужно собирать логи
- Можно установить на любое количество машин (1, 2, 10, 100...)
- Каждая машина должна иметь доступ к серверу по сети

### 3.2. Для каждой машины-агента выполните:

#### 3.2.1. Перенесите файлы на машину агента
Скопируйте на машину агента:
- `main.py`
- `requirements-client.txt`
- Папку `client/` (со всеми файлами внутри)

**Структура на машине агента должна быть:**
```
/путь/к/проекту/
  ├── main.py
  ├── requirements-client.txt
  └── client/
      ├── agent.py
      ├── connection.py
      └── gui.py
```

#### 3.2.2. Проверьте Python на машине агента
```bash
# На Linux/macOS:
python3 --version

# На Windows:
python --version
```
**Требуется:** Python 3.10 или выше

**⚠️ Для Ubuntu 22.04:** Если pip не установлен (ошибка `No module named pip`), см. раздел 2.3 для инструкций по установке.

#### 3.2.3. Установите зависимости на машине агента
```bash
# На Linux/macOS:
cd /путь/к/проекту
python3 -m pip install -U pip
python3 -m pip install -U -r requirements-client.txt

# На Windows:
cd C:\путь\к\проекту
python -m pip install -U pip
python -m pip install -U -r requirements-client.txt

# Опционально для Windows (для лучшей производительности):
pip install pywin32
```

#### 3.2.4. Запустите агента

**На Linux-машине:**
```bash
python3 main.py agent --server http://<IP-сервера>:8080 --source journal --interval 60
```

**На Windows-машине:**
```powershell
python main.py agent --server http://<IP-сервера>:8080 --source eventlog --interval 60
```

**Параметры:**
- `--server http://<IP-сервера>:8080` - замените `<IP-сервера>` на IP из шага 2.6 (например, `http://192.168.1.100:8080`)
- `--source` - источник логов:
  - `auto` - автоматическое определение (рекомендуется)
  - `journal` - Linux journalctl
  - `eventlog` - Windows Event Log
  - `file` - файлы логов (Linux)
- `--limit 200` - количество событий за раз (по умолчанию 200)
- `--interval 60` - интервал отправки в секундах (по умолчанию 60)

**Ожидаемый вывод:**
```
Agent started. Collecting logs from: eventlog
Sending to: http://192.168.1.100:8080
Interval: 60 seconds
Press Ctrl+C to stop

[2024-01-15 10:30:00] Sent 45 logs, saved 45, skipped 0
[2024-01-15 10:31:00] Sent 12 logs, saved 12, skipped 0
```

#### 3.2.5. Особенности для Linux-агентов
Если возникают проблемы с доступом к journalctl:
```bash
# Добавьте пользователя в группу systemd-journal
sudo usermod -aG systemd-journal $USER
# Перезайдите в систему для применения изменений
```

#### 3.2.6. Особенности для Windows-агентов
- Если `pywin32` не установлен, агент использует PowerShell (работает, но медленнее)
- Для некоторых событий может потребоваться запуск от имени администратора

---

## 💻 Шаг 4: Настройка КЛИЕНТА-АУДИТОРА (1 машина)

> **💡 Примечание:** Начиная с версии 0.2, система включает веб-дашборд, доступный через браузер. 
> Десктопный GUI клиент (PySide6) по-прежнему доступен, но веб-дашборд рекомендуется для большинства случаев использования.

### 4.1. Выберите способ просмотра логов

**Вариант A: Веб-дашборд (рекомендуется)**
- Откройте браузер и перейдите по адресу `http://<IP-сервера>:8080`
- Не требует установки дополнительного ПО
- Работает на любой платформе
- Современный интерфейс с графиками и метриками

**Вариант B: Десктопный GUI клиент**
- Требует установки PySide6
- Работает как отдельное приложение
- Подходит для случаев, когда нужен офлайн-доступ

### 4.2. Использование веб-дашборда

Просто откройте в браузере:
```
http://<IP-сервера>:8080
```

Веб-дашборд автоматически загрузит данные и отобразит:
- Ключевые метрики в карточках
- Интерактивные графики
- Таблицу последних событий

**Фильтрация в веб-дашборде:**
- Используйте поле "Search" для поиска по содержимому
- Выберите уровень важности в выпадающем списке
- Нажмите "Refresh" для обновления данных

### 4.3. Настройка десктопного GUI клиента (опционально)

Если вы хотите использовать десктопный GUI вместо веб-дашборда:

#### 4.3.1. Выберите машину для клиента
- Может быть та же машина, что и сервер
- Может быть отдельная машина
- Должна иметь доступ к серверу по сети

#### 4.3.2. Перенесите файлы на машину клиента
Скопируйте на машину клиента:
- `main.py`
- `requirements-client.txt`
- Папку `client/` (со всеми файлами внутри)

**Структура на машине клиента должна быть:**
```
/путь/к/проекту/
  ├── main.py
  ├── requirements-client.txt
  └── client/
      ├── connection.py
      └── gui.py
```

#### 4.3.3. Проверьте Python на машине клиента
```bash
# На Linux/macOS:
python3 --version

# На Windows:
python --version
```
**Требуется:** Python 3.10 или выше

**⚠️ Для Ubuntu 22.04:** Если pip не установлен (ошибка `No module named pip`), см. раздел 2.3 для инструкций по установке.

#### 4.3.4. Установите зависимости на машине клиента
```bash
# На Linux/macOS:
cd /путь/к/проекту
python3 -m pip install -U pip
python3 -m pip install -U -r requirements-client.txt

# На Windows:
cd C:\путь\к\проекту
python -m pip install -U pip
python -m pip install -U -r requirements-client.txt
```

#### 4.3.5. Запустите клиент-аудитор

**На Windows:**
```powershell
python main.py client --server http://<IP-сервера>:8080
```

**На Linux/macOS:**
```bash
python3 main.py client --server http://<IP-сервера>:8080
```

**Параметры:**
- `--server http://<IP-сервера>:8080` - замените `<IP-сервера>` на IP из шага 2.6 (например, `http://192.168.1.100:8080`)

#### 4.3.6. Использование GUI клиента
После запуска откроется графический интерфейс с двумя вкладками:

**Вкладка "Logs" (Логи):**
- Фильтрация по хосту (машине-агенту)
- Фильтрация по уровню важности (severity)
- **Поиск по содержимому сообщений** - введите текст в поле "Search" для поиска в сообщениях логов
- **Фильтр по времени** - выберите период:
  - "All time" - все время
  - "Last hour" - последний час
  - "Last 6 hours" - последние 6 часов
  - "Last 24 hours" - последние 24 часа
  - "Last 7 days" - последние 7 дней
  - "Custom..." - произвольная дата и время
- Настройка лимита количества отображаемых событий
- Отображение логов в таблице
- **Двойной клик по строке** - открывает окно с детальной информацией о событии
- Кнопка "Dashboard" - переключается на вкладку с графиками

**Вкладка "Dashboard" (Дашборд):**
- **График распределения по severity** - круговая диаграмма с цветовой индикацией
- **График распределения по хостам** - столбчатая диаграмма топ-10 хостов
- **График временной линии** - линейный график событий за последние 24 часа
- Кнопка "Refresh Dashboard" - обновляет все графики

---

## 📊 Шаг 5: Проверка работы системы

### 5.1. Проверьте, что все компоненты запущены
- ✅ Сервер работает (шаг 2.5)
- ✅ Агенты запущены на всех нужных машинах (шаг 3.2.4)
- ✅ Веб-дашборд доступен в браузере: `http://<IP-сервера>:8080` (шаг 2.8)
- ✅ (Опционально) Десктопный клиент-аудитор запущен (шаг 4.3.5)

### 5.2. Проверьте сбор логов

**В веб-дашборде:**
1. Откройте `http://<IP-сервера>:8080` в браузере
2. Проверьте карточки метрик - должны отображаться числа
3. Проверьте графики - должны быть видны данные
4. Проверьте таблицу событий - должны отображаться логи от всех агентов

**В десктопном GUI клиенте (если используется):**
1. Нажмите кнопку "Stats" - должна отображаться статистика
2. Проверьте фильтры - убедитесь, что они не скрывают логи
3. Должны отображаться логи от всех агентов

### 5.3. Проверьте подключение агентов к серверу
В окне агента должны появляться сообщения:
```
[2024-01-15 10:30:00] Sent 45 logs, saved 45, skipped 0
```

Если сообщений нет или есть ошибки - см. раздел "Решение проблем".

---

## 🔧 Решение проблем

### Проблема: "python: command not found"

**Решение:**
- На Linux/macOS используйте `python3` вместо `python`
- Убедитесь, что Python установлен и добавлен в PATH

### Проблема: "ModuleNotFoundError"

**Решение:**
1. Убедитесь, что вы установили зависимости:
   - Для сервера: `pip install -r requirements-server.txt`
   - Для агента/клиента: `pip install -r requirements-client.txt`
2. Проверьте, что используете правильную версию Python (3.10+)
3. Убедитесь, что вы находитесь в правильной директории с файлами

### Проблема: Агент не может подключиться к серверу

**Ошибка:** `Connection refused` или `Failed to establish a new connection`

**Пошаговая диагностика:**

#### Шаг 1: Проверьте, что сервер запущен
На машине сервера выполните:
```bash
# Проверьте, запущен ли сервер
ps aux | grep "main.py server"
# или
netstat -tuln | grep 8080
```

Если сервер не запущен, запустите его:
```bash
python3 main.py server --host 0.0.0.0 --port 8080
```

#### Шаг 2: Проверьте IP-адрес сервера
На машине сервера выполните:
```bash
# Узнайте IP-адрес сервера
hostname -I
# или
ip addr show | grep "inet "
```

**⚠️ ВАЖНО для VirtualBox:**
- Если обе VM используют **NAT** - они **не видят друг друга**!
- Используйте **Bridged Adapter** или **Internal Network** для связи между VM
- IP вида `10.0.2.x` - это NAT, VM не могут общаться напрямую

#### Шаг 3: Проверьте доступность сервера с машины агента
На машине агента выполните:
```bash
# Проверьте доступность сервера
curl http://<IP-сервера>:8080/health

# Если curl не установлен:
sudo apt install curl
```

**Ожидаемый ответ:** `{"status":"ok"}`

Если не работает:
- Проверьте, что IP правильный
- Проверьте настройки сети в VirtualBox

#### Шаг 4: Настройка сети в VirtualBox (если сервер и агент на разных VM)

**Вариант A: Bridged Adapter (рекомендуется)**
1. В VirtualBox выберите VM → Настройки → Сеть
2. Адаптер 1 → Тип подключения: **Сетевой мост (Bridged Adapter)**
3. Примените для обеих VM (сервер и агент)
4. Перезапустите VM
5. Узнайте новые IP-адреса: `hostname -I` на каждой VM
6. Используйте новый IP сервера в команде агента

**Вариант B: Internal Network**
1. В VirtualBox выберите VM → Настройки → Сеть
2. Адаптер 1 → Тип подключения: **Внутренняя сеть (Internal Network)**
3. Имя сети: `audit-network` (одинаковое для обеих VM)
4. Перезапустите VM
5. Узнайте IP-адреса: `hostname -I`
6. Используйте IP сервера в команде агента

#### Шаг 5: Проверьте файрвол
На машине сервера:
```bash
# Ubuntu/Debian
sudo ufw status
# Если файрвол активен, откройте порт:
sudo ufw allow 8080/tcp
```

#### Шаг 6: Убедитесь, что сервер запущен с правильными параметрами
Сервер должен быть запущен с `--host 0.0.0.0`:
```bash
python3 main.py server --host 0.0.0.0 --port 8080
```

**Неправильно:** `--host 127.0.0.1` (доступен только локально)

#### Шаг 7: Если сервер и агент на одной машине
Используйте `127.0.0.1` или `localhost`:
```bash
python3 main.py agent --server http://127.0.0.1:8080 --source journal --interval 60
```

### Проблема: "Access denied" при чтении логов на Linux

**Решение:**
```bash
# Добавьте пользователя в группу systemd-journal
sudo usermod -aG systemd-journal $USER
# Перезайдите в систему для применения изменений
```

### Проблема: Windows Event Log не работает

**Решение:**
1. Установите `pywin32`: `pip install pywin32`
2. Если не помогает, попробуйте запустить от имени администратора
3. Проверьте, что PowerShell доступен

### Проблема: Клиент не видит логи

**Решение:**
1. Убедитесь, что агенты запущены и отправляют логи (см. шаг 3.2.4)
2. Проверьте, что сервер работает и принимает логи
3. **Для веб-дашборда:** Откройте `http://<IP-сервера>:8080` и проверьте метрики
4. **Для GUI клиента:** Нажмите "Stats" - должна отображаться статистика
5. Проверьте фильтры (может быть установлен фильтр по хосту/severity)
6. Подождите несколько минут - логи отправляются с интервалом (по умолчанию 60 секунд)
7. Обновите страницу веб-дашборда (F5) или нажмите "Refresh" в GUI

### Проблема: Файлы не найдены при запуске

**Решение:**
1. Убедитесь, что вы скопировали все нужные файлы (см. шаги 2.2, 3.2.1, 4.3.2)
2. Проверьте структуру папок:
   - На сервере должны быть папки `server/` и `web/`
   - На клиенте должна быть папка `client/`
3. Убедитесь, что вы запускаете команды из правильной директории (где находится `main.py`)

### Проблема: Веб-дашборд не открывается

**Решение:**
1. Убедитесь, что сервер запущен (см. шаг 2.5)
2. Проверьте, что папка `web/` присутствует на сервере со всеми файлами
3. Проверьте доступность сервера: `curl http://<IP-сервера>:8080/health`
4. Убедитесь, что используете правильный IP-адрес и порт
5. Проверьте файрвол - порт 8080 должен быть открыт
6. Попробуйте открыть в другом браузере или в режиме инкогнито

---

## 📝 Краткая шпаргалка команд

### Сервер:
```bash
# Linux/macOS:
python3 main.py server --host 0.0.0.0 --port 8080

# Windows:
python main.py server --host 0.0.0.0 --port 8080
```

### Агент (Linux):
```bash
python3 main.py agent --server http://<IP-сервера>:8080 --source journal --interval 60
```

### Агент (Windows):
```powershell
python main.py agent --server http://<IP-сервера>:8080 --source eventlog --interval 60
```

### Веб-дашборд:
```
Откройте в браузере: http://<IP-сервера>:8080
```

### Клиент-аудитор (десктопный GUI, опционально):
```bash
# Linux/macOS:
python3 main.py client --server http://<IP-сервера>:8080

# Windows:
python main.py client --server http://<IP-сервера>:8080
```

---

## 🔒 Безопасность

⚠️ **Важно:** 
- Сервер предназначен для приема логов от агентов; не выставляйте его в публичный доступ без сетевых ограничений
- Рекомендуется использовать только в локальной сети
- Для production используйте HTTPS (настройте reverse proxy)
- Рассмотрите возможность добавления аутентификации

---

## 📞 Дополнительная помощь

Если возникли проблемы:
1. Проверьте версию Python: `python --version` (должна быть 3.10+)
2. Убедитесь, что все зависимости установлены
3. Проверьте логи ошибок в терминале
4. Убедитесь, что порты не заблокированы файрволом
5. Проверьте, что сервер доступен: откройте `http://<IP-сервера>:8080/health` в браузере
6. Для веб-дашборда: убедитесь, что папка `web/` присутствует на сервере со всеми файлами

---

## 🎯 API эндпоинты сервера

Для разработчиков:

- `GET /` - главная страница веб-дашборда (HTML)
- `GET /health` - проверка доступности сервера
- `POST /logs` - отправка логов от агентов (используется агентами)
- `GET /logs?host=...&severity=...&since=...&search=...&limit=...&offset=...` - получение логов с фильтрацией (используется клиентом и веб-дашбордом)
  - `host` - фильтр по хосту
  - `severity` - фильтр по уровню важности (err, warn, info, debug и т.д.)
  - `since` - фильтр по времени (ISO формат, например: "2024-01-15T10:00:00")
  - `search` - поиск по содержимому сообщения (LIKE поиск)
  - `limit` - максимальное количество событий (1-1000)
  - `offset` - смещение для пагинации
- `GET /stats` - статистика по логам
- `GET /static/*` - статические файлы (CSS, JS) для веб-дашборда

Примеры:

```bash
# Проверка здоровья
curl http://localhost:8080/health

# Получение статистики
curl http://localhost:8080/stats

# Получение логов с фильтром по severity
curl "http://localhost:8080/logs?limit=10&severity=err"

# Поиск по содержимому
curl "http://localhost:8080/logs?search=error&limit=50"

# Фильтр по времени (последний час)
curl "http://localhost:8080/logs?since=2024-01-15T10:00:00"

# Комбинированные фильтры
curl "http://localhost:8080/logs?host=server1&severity=err&search=failed&limit=100"
```
